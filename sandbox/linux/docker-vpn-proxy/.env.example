# Absolute path to the .ovpn file on the host
OVPN_FILE=/path/to/client.ovpn

# OpenVPN credentials (leave blank if embedded in the .ovpn file via auth-user-pass inline)
VPN_USER=
VPN_PASSWORD=

# Port forward 1
HOST_PORT_1=1234
TARGET_HOST_1=10.8.0.2
TARGET_PORT_1=5678
